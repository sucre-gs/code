#include <stdio.h>

int main(void)
{
	int num = 0;
	int a = 1;
	int b = 2;

	printf("num = %d; a = %d; b = %d\n", num, a , b);
	printf("num = a + b : %d; a + 1 : %d; b + 2 : %d\n", a + b, a + 1, b + 2);

	return 0;
}
